services:
  - type: web
    name: btc-predictor
    runtime: python
    disk:
      name: btc-data
      mountPath: /data
      sizeGB: 1
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT --ws websockets
